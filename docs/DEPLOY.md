# Deployment

## Local dev
1. Start MongoDB and PostgreSQL.
2. `cd backend && npm install && npm run dev`
3. `cd rn-us-platform && npm install && npm run dev`

## Production ideas
- Deploy frontend to Vercel/Netlify.
- Deploy backend to Railway/Render.
- Use managed MongoDB Atlas + managed Postgres for analytics snapshots.
